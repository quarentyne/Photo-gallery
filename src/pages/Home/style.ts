import styled from "styled-components";
import { FONT_SIZES } from "../../shared/constants/themes";

export const StyledErrorMessage = styled.div`
  font-size: ${FONT_SIZES[18]};
  height: 20vh;
  display: flex;
  justify-content: center;
  align-items: end;
`;